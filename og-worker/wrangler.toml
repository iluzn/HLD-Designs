name = "systemcraft-og"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
ENVIRONMENT = "production"
# Base URL of your self-hosted Piston instance (include the /api/v2 suffix).
# Example: "https://piston.systemcraft.in/api/v2"
# Leave empty until you have deployed Piston; /execute returns 503 until set.
PISTON_URL = ""
